const { computedOccurNumber } = require('../function/array');

const arr = ['akashi', 'asuka', 'nanasei', 'ayame', 'shiori', 'mayu', 'akashi'];
const res = computedOccurNumber(arr);
console.log('res', res);
